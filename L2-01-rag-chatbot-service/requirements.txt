fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
pypdf==4.3.1
python-docx==1.1.2
pydantic==2.9.2

# Vector store
chromadb==0.5.15

# Local embeddings (used when EMBEDDING_PROVIDER=local, the default).
# Pulls in torch -- this is the heaviest dependency in the project.
# Set EMBEDDING_PROVIDER=gemini instead to skip this and stay lightweight
# (important on RAM-constrained free hosting tiers).
sentence-transformers==3.1.1

# LLM + embedding provider SDKs
groq==0.11.0
openai==1.51.0
google-generativeai==0.8.3

# Frontend
streamlit==1.38.0
requests>=2.33.0

# Tests
pytest==8.3.3
httpx==0.27.2
